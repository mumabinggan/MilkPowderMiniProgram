<!--components/settlement/settlementproductgroup/index.wxml-->
<view class="container">
  <text class="title">商品信息</text>
  <v-product-item wx:for="{{products}}" 
                    wx:key="{{item.id}}"
                    class="item"
                    item="{{item}}"
                    catch:tap="onAddressItem"
                    data-id="{{item.id}}">
  </v-product-item>
</view>
